<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>home</title>
    <link rel="stylesheet" href="../css/style.css">
</head>
<body>
    <ul>
        <div id="li-1"><li><a class="active" href="home_admin">Home</a></li></div>
        <div id="li-2"><li><a href="home_admin">ข้อมูลสนาม</a></li></div>
        <div id="li-2"><li><a href="sale-admin">ข้อมูลการขาย</a></li></div>
        <div id="li-2"><li><a href="datamem_admin">ข้อมูลสมาชิก</a></li></div>
        <div id="li-2"><li><a href="logout">ออกจากระบบ</a></li></div>
    </ul>
    <center>
        <div class="type">
            <div class="htype">
                <h2>ข้อมูลการขาย</h2>
            </div>
        </div>
        <div class="tables">
            <table>
                <tr>
                  <th>รหัสการขาย</th>
                  <th>ชื่อสนาม</th>
                  <th>ประเภทสนาม</th>
                  <th>เวลาเช่า</th>
                  <th>วันที่เช่า</th>
                  <th>ราคา</th>
                  <th>รหัสสมาชิก</th>
                  
                </tr>
                <%for(let i=0; i<sales_data.length;i++) { %>
                <tr>
                  <td><%= sales_data[i].SalesID%></td>
                  <td><%= sales_data[i].stadium.StadiumName%></td>
                  <td><%= sales_data[i].stadium.type_stadium.TypeName%></td>
                  <td><%= sales_data[i].Time%></td>
                  <td><%= new Date(sales_data[i].SaleDate).toLocaleDateString('en-US', {
                    year: 'numeric',
                    month: 'short',
                    day: 'numeric'
                  }); %></td>
                  <td><%= sales_data[i].stadium.Price%></td>
                  <td><%= sales_data[i].user.UserName%></td>
                </tr>
                <% } %>
              </table>
        </div>
    </center>
    
    
</body>
</html>